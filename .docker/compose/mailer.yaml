services:
    mailer:
        image: ${MAILER_IMAGE}
        container_name: ${MAILER_CONTAINER_NAME}
        ports:
            - "1025"
            - "8025:8025"
        environment:
            MP_SMTP_AUTH_ACCEPT_ANY: 1
            MP_SMTP_AUTH_ALLOW_INSECURE: 1
